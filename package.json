{
  "name": "nexo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/client",
    "apps/api"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean --force && rm -rf node_modules",
    "client": "bun run --filter client dev",
    "api": "bun run --filter api dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "bun@1.1.0",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "force": "^0.0.3"
  }
}
