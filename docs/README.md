# Docs - Nexo

## Variables de entorno (cliente)

Las variables de staging y production se configuran en EAS (EAS Secrets) para los builds. Los archivos `.env.staging` y `.env.production` están en `.gitignore`; si necesitas usarlos en local, créalos y no se versionarán.
